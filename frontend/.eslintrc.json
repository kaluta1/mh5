{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-console": ["warn", { "allow": ["warn", "error"] }]
  },
  "overrides": [
    {
      "files": ["**/*.test.{ts,tsx}", "**/vitest.setup.ts"],
      "rules": {
        "no-console": "off"
      }
    }
  ]
}
