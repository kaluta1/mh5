# Used by `next dev` when NEXT_PUBLIC_API_URL is not set in `.env.local`.
# Point the browser at your local FastAPI so Admin → Accounting reads the same DB you seed.
NEXT_PUBLIC_API_URL=http://localhost:8000
