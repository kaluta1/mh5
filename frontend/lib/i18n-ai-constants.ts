/** Bump when English source strings change materially so stale AI caches refresh. */
export const I18N_AI_CACHE_VERSION = 1

/** Chunk size for /api/i18n/translate (balance latency vs token limits). */
export const I18N_AI_CHUNK_KEYS = 120
